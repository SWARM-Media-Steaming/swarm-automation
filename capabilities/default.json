{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The automation UI invokes only its own Rust commands; file dialogs and external URLs are wrapped by those commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
